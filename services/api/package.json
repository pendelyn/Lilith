{
  "name": "@lilith/api",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node --env-file-if-exists=.env --watch src/index.ts",
    "typecheck": "tsc --noEmit",
    "test": "node --test src/health.test.ts"
  },
  "dependencies": {
    "@lilith/contracts": "*"
  }
}
